---
tags:
  - python
date: 2025-10-10
layout: post
title: Tkinter - 基本操作、类继承关系与部分组件
---
#tkinter
## 头文件 Header
```python
import tkinter as tk
```

# Tkinter类继承关系
这边给到一个tkinter类的继承关系图

![[Tkinter 组件类继承图.png]]

- **Tk**同时属于**Wm**与**Misc**类
- **Toplevel** 顶级窗口，永远置于最顶层
- **Wm** 负责窗口间的通信
- **Misc** 杂项，所有组件的根父类
- **Pack / Place / Grid** 布局管理器，**所继承的东西**的布局都要用这些东西

#### 值的修改
**Object**都支持__getitem__和__setitem__的做法！(.config()应该也都有)
这样就可以修改Object里面有关的值了

```python
object['item'] = 'value'
object.config(...)
```

# 窗口 window
窗口作为一个对象存在，我们用 **Tk()** 来创建
> [!attention] 一个对象 (Object) 定义时，函数首字母均为大写
```python
window = tk.Tk();
window.title('window') #窗口定义
window.resizeable(True,True)
window.geometry('200x100') #用x代表*，这表示界面尺寸，（长，宽）
window.iconbitmap('.../.ico')

window.configure(bg = "...")
window.attributes('-alpha' , 0.9)
...

window.mainloop() #循环
#window.destroy()
```
- **Tk** : 创建窗口对象
- **title** 窗口名称
- **resizeable** 是否可以调整窗口大小
- **geometry** 给定一个string，里面包含了窗口大小和窗口位置
	- "*width*x*height*±*distance_from_left*±*distance_from_ceiling*"
	- 四个参数分别为**窗口宽度**，**窗口高度**，**距屏幕左边的宽**，**距离屏幕顶部的高**
	- 如果输入值为**None**，则会返回此刻的四个数值
- **iconbitmap** 图标

- **configure** 里面可以设置基本设置 更多请见[[Tkinter - 4 更多组件，更多操作和更多窗口]]
- **attributes** 里面可以设置属性 更多请见[[Tkinter - 4 更多组件，更多操作和更多窗口]]
	- **-alpha** 不透明度
	- **-topmost** 是否指定 (True / False)

- **mainloop** 主循环

- **destroy** 破坏程序，即退出

# 组件
**组件的概念需要搞清楚**，它相当于一个封装好的模块，你可以直接创建一个组件对象，然后放到屏幕上去

## 标签组件 Label
标签作为一个**组件**存在，我们用 **Label()** 来创建，实际上是用来显示文本的hh
```python
label_1 = tk.Label(window , text = 'Hello World' , bg='green' , font = ('Arial' , 12) , width = 15 , height = 2);
```

- **window**：该组件属于的窗口
- **text**：该组件显示的文本
- **background / bg (option参数)**：该组件的背景
	- 相应的还有foreground / fg
- **font (option参数)**：字体，字号
- **width (option参数)**：标签字符宽度（以字符为单位，下同）
- **height (option参数)**：标签字符高读
- **relief**：边框样式
	- 1. **FLAT**：平坦的边框，没有任何效果。
	- 2. **RAISED**：凸起的边框，给人一种立体感。
	- 3. **SUNKEN**：凹陷的边框，呈现出凹下去的效果。
	- 4. **GROOVE**：凹槽边框，具有凹槽的视觉效果。
	- 5. **RIDGE**：脊状边框，类似于凸起的边框，但更为复杂。

## 按钮组件  Button
按钮作为一个**组件**存在，我们用 **Botton()** 来创建，用于创建一个按钮
```python
button_1 = tk.Botton(window , text = 'Switch window size' , width = 15 , height = 2 , command = function_name)
```

- **window**：该组件属于的窗口
-  (option参数略)
- **command** : 与一个函数绑定，作为点击按钮后执行的函数
	- 也可以通过`button_1.bind(function_name)`来绑定

## 输入框组件 Entry / 字符串变量 StringVar / 整数变量 IntVar
输入款作为一个**组件**存在，我们用 **Entry()** 来创建，用于创建一个输入框
字符串变量与Entry搭配使用，用于读取Entry的值

```python
s1 = tk.StringVar()
tk.Entry(root , **option , textvariable = s1 , state = '').place(x,y)
#s1.set('请输入文本')
#s1.get()
```

- **textvariable**：传参到的地方，需要专门对应**Stringvar**类型
- **state**：默认为空，可以是**readonly (只读)**、**dis / disabled  (禁用)**
- **.set方式**：对于**Stringvar**来说，此时他就代表了**Entry**里的内容，所以对**s1的设置**相当于对**Entry里文本**的设置
- **.get方式**：返回一个字符串类型的数据，即目前**所绑定的Entry**里的值

**整数变量**相对于字符串变量，在填入的时候需要多加一个**onvalue / offvalue**的值，表示返回的**int值大小**
当然，**Entry**肯定干不了这事，后面提到的[[Tkinter - 4 更多组件，更多操作和更多窗口#单选框 radiobutton|单选框和复选框]]可以做到

```Python
intvar_1 = tk.IntVar()
intvar_2 = tk.IntVar()
tk.Radiobutton(window , text = '1' , variable = intvar_1 , onvalue = 1 , off_value = -1)
tk.Radiobutton(window , text = '2' , variable = intvar_2 , onvalue = 2 , off_value = -2)
```

> [!attention] 一定要注意，输入值的获取需要用`.get`方法

# 信息框
**信息框 MessageBox**即弹窗信息，通过不同方法可以发送不同类型的弹窗，具体见下

```python
from tkinter import messagebox
#你当然可以直接tkinter.messagebox

messagebox.showinfo('TITLE_INFO' , 'TEXT')
messagebox.showwarning('TITLE_WARNING' , 'TEXT')
messagebox.showerror('TITLE_ERROR' , 'TEXT')
d1 = messagebox.askquestion('YES_OR_NO' , 'TEXT')
d1 = messagebox.askokcancel('OK_OR_CANCEL' , 'TEXT')
d1 = messagebox.askretrycancel('RETRY_OR_CANCEL' , 'TEXT')
d1 = messagebox.askyesno('YES_OR_NO' , 'TEXT')
d1 = messagebpx.askyesnocancel('YES_NO_OR_CANCEL' , 'TEXT')
```

一些方法可以有多个参数
- **title** 标题
- **text** 信息
- **icon** 设置窗口图标 ( INFO、ERROR、 QUESTION、WARNING )
- **default** 设置默认按钮布局
- **parent** 关闭窗口后焦点返回到指定窗口，这里填的就是**对象名**

部分函数有返回值，基本就是True和False，看着办

# \*\*OPTION 参数 
函数定义是给到了\*\*option参数，定义方法见[[Python 6 函数#可变关键字参数]]
以下列出常用的**option**参数

| 参数               | 说明                                                                   |
| ---------------- | -------------------------------------------------------------------- |
| anchor           | 如果空间大于所需时，控制消息的位置，默认是CENTER                                          |
| aspect           | 控件宽度与高度比，默认是150%                                                     |
| bd               | 边界宽度，默认是两个像素                                                         |
| background或者bg   | 背景色彩                                                                 |
| bitmap           | 使用默认位图当做Message内容                                                    |
| cursor           | 当鼠标光标移至按钮上时的形状                                                       |
| foreground 或者 fg | 前景色彩，即字体颜色                                                           |
| font             | 字形、字形样式与大小                                                           |
| heigth           | 高，单位是字符高                                                             |
| image            | Message 以图像方式呈现                                                      |
| justify          | 当有多行文字时，最后一行文字的对齐方式  <br>取值为LEFT/CENTER/RIGHT(靠左/居中/靠右)  <br>默认是居中对齐 |
| padx/pady        | Message 文字与边框的间距，单位是像素                                               |
| relief           | 可由此控制文字外框  <br>默认是relief=FLAT                                        |
| text             | Message 内容，如果有"\n"则可以输入当行文字                                          |
| textvariable     | 文字变量                                                                 |
| underline        | 可以设置第几个文字有下划线，从0开始算起  <br>默认是-1，表示无下划线                               |
| width            | 宽，单位是字符宽                                                             |
| wraplength       | 文本在多少宽度后换行，单位是像素                                                     |

[[Tkinter - 3 OOP化的GUI界面设计]]




